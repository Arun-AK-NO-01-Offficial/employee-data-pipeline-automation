# employee-data-pipeline-automation
An end-to-end ETL automation pipeline that ingests raw employee data, cleanses it, applies business transformations, and loads it into a structured data warehouse layer. The project demonstrates real-world ETL testing scenarios including duplicate handling, null validations, data quality checks, and workflow automation using SQL + Python
